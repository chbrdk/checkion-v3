# Magazine filter / lede cutover (2026-08-03)

Uses `@msqdx/ui` — see `msqdx-ui/knowledge/components/lede-filter-panel.md`.

- Interactive filters: `FilterRow` + `Chip` (projects, domain issues, affected pages, issue rail)
- Snapshot metrics: `Lede` + `LedeStrip`
- Decorative cover/report tag rows may keep local `checkion-chip-row`
