# SEO Market APIs — CHECKION v3

## Status
**Superseded for product UI** by [`seo-project.md`](./seo-project.md) (`/api/projects/:id/seo/*`).

Legacy `/api/seo-market/*` may remain as thin aliases during migration; new code and MCP must use project-scoped routes.
