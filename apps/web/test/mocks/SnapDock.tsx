'use client'

/** SnapDock stub for Vitest (jsdom). */
export function SnapDock({ children }: { children: React.ReactNode }) {
  return <div data-testid="snap-dock-stub">{children}</div>
}

export default SnapDock
