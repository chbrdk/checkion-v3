# Settings — CHECKION v3

## Status
Accepted (Wave 4)

## Goal
Quiet settings page: federation status, display prefs stub, API token stub. No MUI settings drawer.

## Bands
1. **Federation** — contract id, plexon base, health probe link
2. **Appearance** — theme + locale via `paths` keys (client prefs)
3. **Tokens** — personal API token list stub (fixture); create/revoke later

## Auth
Login route stub `/login` — full plexon-v3 session wiring deferred to staging auth slice.
